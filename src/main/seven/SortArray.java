package seven;

import java.util.Arrays;

public class SortArray {

  public static Object[] sortArray(Object[] uai) {
    Arrays.sort(uai);
    return uai;
  }

  public static int[] sortArray(int[] uai) {
    Arrays.sort(uai);
    return uai;
  }

  public static long[] sortArray(long[] uai) {
    Arrays.sort(uai);
    return uai;
  }

  public static double[] sortArray(double[] uai) {
    Arrays.sort(uai);
    return uai;
  }

  public static float[] sortArray(float[] uai) {
    Arrays.sort(uai);
    return uai;
  }

  public static String[] sortArray(String[] uai) {
    Arrays.sort(uai);
    return uai;
  }
}